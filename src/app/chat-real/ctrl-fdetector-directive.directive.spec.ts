import { CtrlFDetectorDirectiveDirective } from './ctrl-fdetector-directive.directive';

describe('CtrlFDetectorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrlFDetectorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
